import React, { Component } from 'react';
import Layout from '../hoc/Layout';

class App extends Component {
  state = {
  }
  render () {
    return (
        <Layout />
    );
  }
}

export default App;
